import { FlexC } from "@/components/containers/flex";

const Tests = () => {
  return (
    <FlexC
      css={{
        background: "$darkPurple",
        width: "900px",
        height: "100vh",
        margin: "auto",
        padding: "60px",
      }}
    ></FlexC>
  );
};

export default Tests;

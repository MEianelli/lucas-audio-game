import { styled } from "@/styles/stitches.config";
import Image from "next/image";
import { VFXImg } from "react-vfx";

export const ImageCss = styled(Image, {});

export const VfxImg = styled(VFXImg);
